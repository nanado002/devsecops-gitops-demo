apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe
  labels:
    app: tic-tac-toe
    environment: staging
spec:
  selector:
    app: tic-tac-toe
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
